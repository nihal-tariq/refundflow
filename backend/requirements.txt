# NOTE: version floors chosen for Python 3.14 wheel availability. The app also
# runs on 3.12/3.13; if you have those, the originally-pinned versions work too.

# --- Web framework ---
fastapi>=0.115.6
uvicorn[standard]>=0.34.0
sse-starlette>=2.1.3

# --- Data / validation ---
pydantic>=2.11
pydantic-settings>=2.7.1
SQLAlchemy>=2.0.36

# --- Agent stack ---
langchain-core>=0.3.28
langgraph>=0.2.60
langgraph-checkpoint>=2.0.9

# --- LLM phrasing layer (optional, provider-agnostic) ---
# Only the package for the *active* LLM_PROVIDER needs to be installed. Without
# one, the app degrades to the deterministic template responder. Default
# provider is Anthropic, so its package ships installed:
langchain-anthropic>=0.3.1
# Uncomment the line matching your LLM_PROVIDER to enable that provider:
# langchain-openai>=0.2          # LLM_PROVIDER=openai or openai_compatible
langchain-groq>=0.2             # LLM_PROVIDER=groq
# langchain-google-genai>=2.0    # LLM_PROVIDER=google_genai
# langchain-mistralai>=0.2       # LLM_PROVIDER=mistral
# langchain-ollama>=0.2          # LLM_PROVIDER=ollama

# --- Observability ---
structlog>=24.4.0

# --- Testing ---
pytest>=8.3.4
pytest-asyncio>=0.25.0
httpx>=0.28.1

# --- Voice agent (optional) ---
# These packages are required for the LiveKit voice interface (voice_agent.py + /voice/token).
# Remove or comment them out if you are not using the voice channel.
livekit-agents[silero]>=1.0
livekit-plugins-ai-coustics
livekit-plugins-turn-detector
livekit-api>=0.7           # required for the /voice/token backend endpoint
